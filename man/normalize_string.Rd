% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{normalize_string}
\alias{normalize_string}
\title{Normalise a string: transliterate, upper-case, trim}
\usage{
normalize_string(x, remove_apostrophes = FALSE, remove_internal_spaces = FALSE)
}
\arguments{
\item{x}{character vector.}

\item{remove_apostrophes}{logical: drop `'` (so `O'BRIEN` becomes `OBRIEN`).}

\item{remove_internal_spaces}{logical: drop all whitespace.}
}
\value{
character vector, `NA` preserved.
}
\description{
Transliteration is the point. A hand-rolled `toupper(trimws(...))` does not
delete accented characters -- it PRESERVES them, so an accented name can
never reach its unaccented spelling by any exact or initial-based route.
}
\details{
Internal whitespace is deliberately left alone: some callers need
`"VAN  DER BERG"` preserved. [name_key()] collapses it, because a join key
needs the opposite.
}
