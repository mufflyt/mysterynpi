# Factory: similarity closure with a bound dictionary

Factory: similarity closure with a bound dictionary

## Usage

``` r
create_nickname_aware_similarity(nickname_dict)
```

## Arguments

- nickname_dict:

  from \[create_nickname_dictionary()\].

## Value

\`function(name1, name2)\` returning the similarity score.
